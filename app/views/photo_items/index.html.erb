<h1>Flickr Photo Stream</h1>
<h2><%=@photostream.title%></h2>
<div class="items">
<% @items.each do |item| %>
	<div class="item">
		
		<%=image_tag(item.media["m"]) %>
		<%=link_to(item.title)%>
	</div>
<% end %>
</div>